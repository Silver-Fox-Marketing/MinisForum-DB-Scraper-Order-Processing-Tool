/* Template Builder Styles - Using Correct Theme Variables */

/* Template Builder Panel - Clean Production Styles */
#template-builder-panel {
    min-height: 600px;
    padding: 20px;
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    max-width: 100%;
}

/* When active, ensure it displays with flex layout */
#template-builder-panel.tab-panel.active {
    display: flex;
    flex-direction: column;
    align-items: center;
    opacity: 1;
    visibility: visible;
}

/* Ensure it's hidden when not active (normal tab behavior) */
#template-builder-panel.tab-panel:not(.active) {
    display: none;
}

/* Template Builder Container - Centered Layout */
.template-builder-container {
    display: flex;
    gap: 20px;
    height: calc(100vh - 200px);
    padding: 20px;
    background: var(--theme-bg-secondary);
    border-radius: 10px;
    width: 100%;
    max-width: 1400px;
    margin: 0 auto;  /* Center the container */
    box-sizing: border-box;
}

/* Template Sidebar */
.template-sidebar {
    width: 350px;
    background: var(--theme-surface);
    border-radius: 10px;
    padding: 20px;
    border: 1px solid var(--theme-border);
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 25px;
}

.template-section {
    background: var(--theme-bg-secondary);
    border-radius: 8px;
    padding: 15px;
    border: 1px solid var(--theme-border-light);
}

.template-section h3 {
    margin: 0 0 15px 0;
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--theme-text-primary);
    display: flex;
    align-items: center;
    gap: 8px;
}

.template-section h4 {
    margin: 15px 0 10px 0;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--theme-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

/* Template List */
.template-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.template-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px;
    background: var(--theme-surface);
    border: 1px solid var(--theme-border);
    border-radius: 6px;
    cursor: pointer;
    transition: all 0.2s ease;
}

.template-item:hover {
    border-color: #fd410d;
    background: var(--theme-surface-elevated);
}

.template-item.active {
    border-color: #fd410d;
    background: var(--theme-surface-elevated);
}

/* Inactive template styling */
.template-item.inactive {
    opacity: 0.6;
    border-style: dashed;
}

.template-item.inactive .template-name {
    font-style: italic;
    color: var(--theme-text-secondary);
}

.template-info {
    flex: 1;
}

.template-name {
    font-weight: 600;
    color: var(--theme-text-primary);
    margin-bottom: 4px;
}

.template-details {
    font-size: 0.8rem;
    color: var(--theme-text-secondary);
}

.template-actions {
    display: flex;
    gap: 4px;
}

.btn-icon {
    background: none;
    border: none;
    padding: 6px;
    border-radius: 4px;
    cursor: pointer;
    color: var(--theme-text-secondary);
    transition: all 0.2s ease;
}

.btn-icon:hover {
    background: var(--theme-bg-secondary);
    color: #fd410d;
}

.btn-icon.danger:hover {
    background: #fee;
    color: #dc2626;
}

/* Template Form */
.template-form {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.form-group {
    display: flex;
    flex-direction: column;
    gap: 6px;
}

.form-group label {
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--theme-text-primary);
}

.form-control {
    padding: 8px 12px;
    border: 1px solid var(--theme-border);
    border-radius: 4px;
    background: var(--theme-surface);
    color: var(--theme-text-primary);
    font-size: 0.9rem;
}

.form-control:focus {
    outline: none;
    border-color: #fd410d;
    box-shadow: 0 0 0 2px rgba(253, 65, 13, 0.1);
}

/* Field Library */
.field-library {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.field-category {
    border-top: 1px solid var(--theme-border);
    padding-top: 15px;
}

.field-category:first-child {
    border-top: none;
    padding-top: 0;
}

.field-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px;
}

.field-item {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 10px;
    background: var(--theme-surface);
    border: 2px solid var(--theme-border);
    border-radius: 6px;
    cursor: grab;
    transition: all 0.2s ease;
    user-select: none;
    font-size: 0.85rem;
    color: var(--theme-text-primary);
}

.field-item:hover {
    border-color: #fd410d;
    background: var(--theme-surface-elevated);
    transform: translateY(-1px);
}

.field-item:active {
    cursor: grabbing;
    transform: scale(0.95);
}

.field-item.dragging {
    opacity: 0.5;
    transform: rotate(5deg);
}

.field-item.field-clicked {
    animation: fieldClick 0.3s ease;
    background: var(--theme-primary) !important;
    color: white !important;
    transform: scale(0.95);
}

@keyframes fieldClick {
    0% {
        transform: scale(1);
    }
    50% {
        transform: scale(0.95);
        background: var(--theme-primary);
    }
    100% {
        transform: scale(1);
    }
}

.field-item i {
    font-size: 14px;
    color: #fd410d;
}

.field-item.special i {
    color: #8b5cf6;
}

.field-item.calculated i {
    color: #10b981;
}

.field-item.combined i {
    color: #f59e0b;
}

/* Template Designer */
.template-designer {
    flex: 1;
    background: var(--theme-surface);
    border-radius: 10px;
    padding: 20px;
    border: 1px solid var(--theme-border);
    display: flex;
    flex-direction: column;
    gap: 20px;
}

.designer-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 15px;
    border-bottom: 1px solid var(--theme-border);
}

.designer-header h3 {
    margin: 0;
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--theme-text-primary);
    display: flex;
    align-items: center;
    gap: 8px;
}

.designer-controls {
    display: flex;
    gap: 10px;
}

/* Button Styles */
.btn {
    padding: 8px 16px;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-size: 0.9rem;
    font-weight: 500;
    transition: all 0.2s ease;
    display: inline-flex;
    align-items: center;
    gap: 6px;
}

.btn-primary {
    background: #fd410d;
    color: white;
}

.btn-primary:hover {
    background: #e8370b;
}

.btn-secondary {
    background: var(--theme-bg-secondary);
    color: var(--theme-text-primary);
    border: 1px solid var(--theme-border);
}

.btn-secondary:hover {
    background: var(--theme-surface-elevated);
}

.btn-success {
    background: #10b981;
    color: white;
}

.btn-success:hover {
    background: #059669;
}

.btn-sm {
    padding: 6px 12px;
    font-size: 0.8rem;
}

/* Template Canvas */
.template-canvas {
    flex: 1;
    background: var(--theme-bg-secondary);
    border: 2px dashed var(--theme-border);
    border-radius: 10px;
    padding: 20px;
    display: flex;
    flex-direction: column;
    gap: 20px;
    min-height: 300px;
}

.canvas-header {
    text-align: center;
}

.canvas-title {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--theme-text-primary);
    margin-bottom: 5px;
}

.canvas-subtitle {
    font-size: 0.9rem;
    color: var(--theme-text-secondary);
}

.template-columns {
    display: flex;
    gap: 15px;
    flex-wrap: wrap;
    justify-content: center;
    align-items: flex-start;
}

.drop-zone {
    min-height: 200px;
    border: 2px dashed var(--theme-border);
    border-radius: 8px;
    padding: 20px;
    background: var(--theme-surface);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

.drop-zone.empty {
    min-width: 300px;
}

.drop-zone.drag-over {
    border-color: #fd410d;
    background: var(--theme-surface-elevated);
    transform: scale(1.02);
}

.drop-message {
    text-align: center;
    color: var(--theme-text-secondary);
}

.drop-message i {
    font-size: 48px;
    margin-bottom: 15px;
    color: #fd410d;
}

.drop-message p {
    margin: 8px 0;
    font-size: 1rem;
    font-weight: 500;
    color: var(--theme-text-primary);
}

.drop-message .help-text {
    font-size: 0.85rem;
    font-weight: normal;
    color: var(--theme-text-secondary);
    opacity: 0.7;
}

/* Template Column */
.template-column {
    min-width: 200px;
    max-width: 300px;
    background: var(--theme-surface);
    border: 1px solid var(--theme-border);
    border-radius: 8px;
    padding: 15px;
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.column-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 10px;
    border-bottom: 1px solid var(--theme-border);
}

.column-title {
    font-weight: 600;
    color: var(--theme-text-primary);
}

.column-controls {
    display: flex;
    gap: 4px;
}

.column-field {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 8px 10px;
    background: var(--theme-bg-secondary);
    border: 1px solid var(--theme-border);
    border-radius: 4px;
    cursor: move;
}

.column-field:hover {
    border-color: #fd410d;
}

.field-info {
    display: flex;
    align-items: center;
    gap: 8px;
    color: var(--theme-text-primary);
}

.field-controls {
    display: flex;
    gap: 4px;
}

/* Template Preview Section */
.template-preview-section {
    background: var(--theme-bg-secondary);
    border-radius: 8px;
    padding: 15px;
    border: 1px solid var(--theme-border);
    max-height: 400px;
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.preview-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 15px;
}

.preview-header h4 {
    margin: 0;
    font-size: 1rem;
    font-weight: 600;
    color: var(--theme-text-primary);
    display: flex;
    align-items: center;
    gap: 8px;
}

.preview-dealership {
    max-width: 250px;
    padding: 6px 10px;
    border: 1px solid var(--theme-border);
    border-radius: 4px;
    background: var(--theme-surface);
    color: var(--theme-text-primary);
}

.preview-table-container {
    flex: 1;
    overflow: auto;
    border: 1px solid var(--theme-border);
    border-radius: 6px;
}

.preview-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.85rem;
}

.preview-table th,
.preview-table td {
    padding: 8px 12px;
    text-align: left;
    border-bottom: 1px solid var(--theme-border);
}

.preview-table th {
    background: var(--theme-surface);
    font-weight: 600;
    color: var(--theme-text-primary);
    position: sticky;
    top: 0;
    z-index: 1;
}

.preview-table td {
    color: var(--theme-text-primary);
}

.preview-table td.no-preview {
    text-align: center;
    padding: 30px;
    color: var(--theme-text-secondary);
    font-style: italic;
}

.preview-table td.no-preview i {
    margin-right: 8px;
    color: #fd410d;
}

/* Responsive Design */
@media (max-width: 1200px) {
    .template-builder-container {
        flex-direction: column;
        height: auto;
    }

    .template-sidebar {
        width: 100%;
        max-height: 400px;
    }

    .field-grid {
        grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    }
}

@media (max-width: 768px) {
    .template-builder-container {
        padding: 10px;
        gap: 15px;
    }

    .template-sidebar,
    .template-designer {
        padding: 15px;
    }

    .designer-header {
        flex-direction: column;
        gap: 10px;
        align-items: stretch;
    }

    .designer-controls {
        justify-content: center;
    }
}

/* Dark mode - variables are already handled by theme system */
[data-theme="dark"] .btn-icon.danger:hover {
    background: rgba(220, 38, 38, 0.1);
    color: #f87171;
}

/* Field Concatenation Modal Styles */

/* Category header for combined fields */
.field-category .category-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 15px;
}

.field-category .category-header h4 {
    margin: 0;
    color: var(--theme-text-primary);
}

/* Field concatenation modal */
#fieldConcatenationModal .modal-content {
    max-width: 800px;
    width: 90vw;
}

/* Field selection area */
.field-selection-area {
    display: flex;
    gap: 20px;
    margin: 15px 0;
    min-height: 250px;
}

.available-fields-list,
.selected-fields-list {
    flex: 1;
    background: var(--theme-bg-secondary);
    border-radius: 8px;
    padding: 15px;
    border: 1px solid var(--theme-border);
}

.available-fields-list h4,
.selected-fields-list h4 {
    margin: 0 0 10px 0;
    color: var(--theme-text-primary);
    font-size: 14px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

/* Field chips for selection */
.field-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}

.field-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 12px;
    background: var(--theme-surface);
    border: 1px solid var(--theme-border);
    border-radius: 20px;
    cursor: pointer;
    transition: all 0.2s ease;
    font-size: 12px;
    color: var(--theme-text-primary);
}

.field-chip:hover {
    background: var(--theme-accent);
    color: white;
    transform: translateY(-1px);
}

.field-chip.selected {
    background: var(--theme-primary);
    color: white;
    border-color: var(--theme-primary);
}

.field-chip i {
    font-size: 11px;
}

.field-chip.dragging {
    opacity: 0.5;
    cursor: grabbing;
}

.field-chip.field-clicked {
    animation: fieldClick 0.3s ease;
    background: var(--theme-primary) !important;
    color: white !important;
    transform: scale(0.95);
}

/* Drag over styles for drop zones */
#selectedFieldChips.drag-over,
#fieldCombinationBuilder.drag-over {
    background: rgba(253, 65, 13, 0.1);
    border-color: #fd410d;
}

/* Field combination builder */
.field-combination-builder {
    min-height: 180px;
    background: var(--theme-surface);
    border: 2px dashed var(--theme-border);
    border-radius: 8px;
    padding: 15px;
    position: relative;
}

.combination-placeholder {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
    color: var(--theme-text-secondary);
    text-align: center;
}

.combination-placeholder i {
    font-size: 24px;
    margin-bottom: 10px;
    opacity: 0.5;
}

.combination-placeholder p {
    margin: 0;
    font-size: 14px;
}

/* Selected field items in builder */
.selected-field-item {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    background: var(--theme-primary);
    color: white;
    border-radius: 20px;
    margin: 4px;
    font-size: 12px;
    position: relative;
}

.selected-field-item .remove-field {
    background: rgba(255, 255, 255, 0.2);
    border: none;
    color: white;
    border-radius: 50%;
    width: 18px;
    height: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    font-size: 10px;
    transition: background 0.2s ease;
}

.selected-field-item .remove-field:hover {
    background: rgba(255, 255, 255, 0.3);
}

/* Field preview */
.field-preview {
    background: var(--theme-bg-secondary);
    border: 1px solid var(--theme-border);
    border-radius: 6px;
    padding: 12px;
    min-height: 40px;
    display: flex;
    align-items: center;
    font-family: 'Courier New', monospace;
    font-size: 14px;
    color: var(--theme-text-primary);
}

.field-preview .preview-text {
    color: var(--theme-text-secondary);
    font-style: italic;
}

.field-preview .preview-sample {
    background: var(--theme-surface);
    padding: 4px 8px;
    border-radius: 4px;
    border: 1px solid var(--theme-border);
    font-weight: 500;
}

/* Field combination display with separators */
.field-combination-display {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 5px;
}

.field-separator {
    color: var(--theme-text-secondary);
    font-weight: bold;
    margin: 0 2px;
}

/* Dark mode enhancements */
[data-theme="dark"] .field-chip:hover {
    background: var(--theme-accent);
    color: white;
}

[data-theme="dark"] .field-preview .preview-sample {
    background: rgba(255, 255, 255, 0.05);
}

/* Custom combined field styling */
.field-item.custom-combined {
    position: relative;
    padding-right: 30px;
}

.field-item.custom-combined .remove-combined-field {
    position: absolute;
    top: 2px;
    right: 2px;
    background: rgba(220, 38, 38, 0.8);
    border: none;
    color: white;
    border-radius: 50%;
    width: 16px;
    height: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    font-size: 8px;
    transition: all 0.2s ease;
    opacity: 0;
    pointer-events: none;
}

.field-item.custom-combined:hover .remove-combined-field {
    opacity: 1;
    pointer-events: auto;
}

.field-item.custom-combined .remove-combined-field:hover {
    background: rgba(220, 38, 38, 1);
    transform: scale(1.1);
}